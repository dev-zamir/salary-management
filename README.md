# Salary Management Tool

A minimal yet usable salary management application for an HR Manager of a
~10,000-employee organization. Built as part of the **Software Craftsperson /
Python / DevOps** assessment.

> **Persona:** HR Manager — needs to manage the employee roster and understand
> salary distribution across countries and job titles.

---

## Table of Contents

1. [Overview](#overview)
2. [Tech Stack](#tech-stack)
3. [Why This Stack](#why-this-stack)
4. [Repository Layout](#repository-layout)
5. [Architecture](#architecture)
6. [Getting Started](#getting-started)
7. [Seeding 10,000 Employees](#seeding-10000-employees)
8. [Running Tests](#running-tests)
9. [Features](#features)
10. [Design Decisions & Trade-offs](#design-decisions--trade-offs)
11. [AI Tooling](#ai-tooling)
12. [Demo](#demo)
13. [Artifacts](#artifacts)

---

## Overview

The application lets an HR Manager:

- **Manage employees** — create, view, update, and delete employees through a
  web UI.
- **Explore salary insights** — view min / max / average salary per country,
  average salary by job title within a country, and additional metrics useful
  for workforce planning.

The solution is built end-to-end: a Rails API backend, a React single-page
frontend, a PostgreSQL database, and a performant seed script capable of
loading 10,000 employees quickly and repeatably.

## Tech Stack

| Layer       | Choice                          |
| ----------- | ------------------------------- |
| Backend     | Ruby on Rails (API mode)        |
| Frontend    | React (Vite) + MUI (Material UI)       |
| Database    | PostgreSQL                      |
| Tests (BE)  | RSpec + FactoryBot              |
| Tests (FE)  | Vitest + React Testing Library  |
| Deploy      | Render (blueprint in `render.yaml`) |

## Why This Stack

The JD lists Python / Go / Java as preferred backend languages. The assessment
explicitly allows "any other framework of your choice," and I've deliberately
chosen **Rails + React + PostgreSQL** for the following reasons:

- **Velocity with quality** — Rails' conventions let me spend time on design,
  testing, and product thinking rather than scaffolding boilerplate.
- **Strong fit for the problem** — the core use case is CRUD + aggregation
  queries over a single relational table, which is Rails + ActiveRecord's
  sweet spot.
- **PostgreSQL over SQLite** — while SQLite is explicitly allowed, Postgres
  gives realistic concurrency semantics, better aggregation performance, and
  is closer to what the tool would actually run on in production.
- **React on the frontend** — directly aligned with the assessment's
  requirement (ReactJS or NextJS).

Trade-offs and the reasoning behind this choice are discussed in more detail
in [`docs/decisions.md`](docs/decisions.md).

## Repository Layout

```
.
├── README.md              # You are here
├── docs/                  # Design notes, ADRs, AI prompts, performance notes
│   ├── architecture.md
│   ├── decisions.md
│   ├── ai-prompts.md
│   └── performance.md
├── backend/               # Rails API application
│   └── README.md          # Backend-specific setup & dev notes
├── frontend/              # React application
│   └── README.md          # Frontend-specific setup & dev notes
└── render.yaml            # Render deployment blueprint
```

## Architecture

```
[ React SPA ] ──HTTP/JSON──▶ [ Rails API ] ──▶ [ PostgreSQL ]
```

Full component breakdown, data flows, and index strategy live in
[`docs/architecture.md`](docs/architecture.md).

## Getting Started

> Full setup instructions will live in the backend and frontend READMEs.
> This section captures the high-level flow.

**Prerequisites**

- Ruby (version in `backend/.ruby-version`)
- Node.js 20+
- PostgreSQL 15+ running locally

**Quick start**

```bash
# 1. Backend
cd backend
bundle install
bin/rails db:setup
bin/rails db:seed          # see "Seeding" below
bin/rails s                # http://localhost:3000

# 2. Frontend (in a second terminal)
cd frontend
npm install
npm run dev                # http://localhost:5173
```

## Seeding 10,000 Employees

The assessment calls out that **the seed script is run regularly and
performance matters**. Names are generated by combining values from
`first_names.txt` and `last_names.txt`.

Key performance considerations:

- Use **bulk insert** (`insert_all` / `upsert_all`) instead of per-row
  `Employee.create` — this is the single biggest lever.
- Load name files once, generate in memory, insert in batches (e.g. 1,000
  rows per statement).
- Wrap in a single transaction to minimize WAL / fsync overhead.
- Make the script **idempotent** and **deterministic** where helpful (seeded
  RNG) so engineers get reproducible data.

Benchmarked at **~2 seconds for 10,000 rows** on a local PostgreSQL.
Methodology and EXPLAIN verification for query indexes are in
[`docs/performance.md`](docs/performance.md).

## Running Tests

```bash
# Backend
cd backend && bundle exec rspec

# Frontend
cd frontend && npm test
```

Test philosophy:

- **Fast** — unit tests avoid hitting the DB where it isn't needed.
- **Deterministic** — no time/order/network flakiness.
- **Meaningful** — cover the behaviour that matters (CRUD rules, aggregation
  correctness, edge cases like empty countries or single-employee groups),
  not line-coverage theatre.

## Features

**Employees**
- List with server-side pagination, sorting, and filtering
- Numbered page navigation with jump-to-page; per-page preference
  persisted in localStorage
- Search across name, email, and job title (debounced)
- Filter dropdowns for country and job title
- Row click opens a detail dialog with all employee fields
- Create, edit, and delete via form dialog with server-side validation
- Currency auto-fills from country but remains editable
- Unsaved-changes warning when closing the form dialog
- Success snackbars after each mutation

**Salary Insights**
- Summary cards: total employees, countries, avg headcount per country
- Country table: min / max / avg salary per country, with currency
- Job title table: breakdown within a selected country (click a
  country row to drill down, with auto-scroll to the detail table)
- Paginated client-side tables (5 / 10 / 25 per page)

## Design Decisions & Trade-offs

Captured as lightweight ADRs in [`docs/decisions.md`](docs/decisions.md).
Topics include:

- Rails + React + Postgres over Python/FastAPI (ADR-001)
- PostgreSQL over SQLite (ADR-002)
- API-only Rails vs full-stack Rails with Hotwire (ADR-003)
- MUI for the component library (ADR-004)
- Server-side pagination and sorting (ADR-005)
- Bulk-insert strategy for the seed script (ADR-006)
- Where aggregation lives (DB vs application layer) (ADR-007)
- Single git repository for backend and frontend (ADR-008)
- Money representation (integer cents) and currency scope (ADR-009)
- API versioning under `/api/v1/` (ADR-010)

## AI Tooling

The assessment explicitly expects intentional use of AI tools. The prompts,
workflows, and notes on where AI helped (and where it didn't) are captured in
[`docs/ai-prompts.md`](docs/ai-prompts.md).

## Demo

**Video walkthrough:** https://share.zight.com/12uYLDjN

The video covers:
- Employee list: pagination, sorting, search, filters, detail view
- Create / edit / delete flows with validation
- Salary Insights: country-level stats and job title breakdown

## Artifacts

All supporting material lives in [`docs/`](docs/):

- `architecture.md` — component diagram and data flow
- `decisions.md` — ADR-style design decisions and trade-offs
- `ai-prompts.md` — AI tool usage notes and representative prompts
- `performance.md` — seed script benchmarks and tuning notes
